# URLFor Functionality

The authoritative reference for URLFor lives in [`skills/structpages/SKILL.md`](../skills/structpages/SKILL.md) §3 "URL Generation" — it covers the recommended call shape, the `[]any` chain disambiguation form, `Ref` qualified paths, strict-by-default behaviour, plus the validation pattern (typed helpers + boot-time `validateURLs` + integration test).

For the API signature, see [`api.md`](./api.md#urlfor).

For a runnable end-to-end demonstration of every URLFor form against a real page tree, see [`examples/url-validation/`](../examples/url-validation/).
